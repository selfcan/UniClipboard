[package]
name = "uc-daemon-contract"
version.workspace = true
license.workspace = true
edition = "2021"
description = "Daemon transport contracts shared by clients and server"

[lib]
name = "uc_daemon_contract"
path = "src/lib.rs"

[dependencies]
uc-core = { path = "../uc-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_with = "3.18.0"
chrono = { version = "0.4", features = ["serde"] }
utoipa = { version = "5", features = ["uuid", "chrono", "url"] }
# Pure semver comparison for the daemon health classifier's strictly-newer
# downgrade guard (ADR-008 P5-L L2). semver is a leaf pure-Rust dep — it does
# NOT pull iroh/diesel, so it keeps this contract crate client-safe.
semver = "1"
[lints]
workspace = true
